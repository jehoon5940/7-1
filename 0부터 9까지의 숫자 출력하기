int pin = 2; 
int num = 0;
void setup() {
    for (int i = 2; i <= 9; i++) {
        pinMode(i, OUTPUT);
    }
    digitalWrite(9, HIGH); 
}
void loop() {
    for(int i = 0; i < 8; i++) {
        digitalWrite(pin+i, numbers[num][i]);
    }
    num++;
    if (num > 9) num = 0;
    delay(1000);
}
